#!/bin/sh

soundfailed="/home/haarp/misc/mech-criticalhitheatsink.wav"
shutdowninit="/home/haarp/bin/shutdowninit"
inittempfile="/tmp/shutdowninit"

if [ ! -f "$inittempfile" ]; then
	#beep -f 1000 -l 400 -n -f 950 -l 400 -n -f 900 -l 400
	beep -f 1000 -l 150 -D 100 -n -f 1000 -l 150 -D 100 -n -f 1000 -l 150 -D 100 -n -f 1000 -l 150 -D 100 -n -f 1000 -l 150 -D 100

	#OLDVOL=$(amixer -c 0 get 'Master' | tail -n1 | awk '{print $3}')
	#amixer -c 0 set 'Master' 100%
	/usr/bin/aplay "$soundfailed"
	#amixer -c 0 set 'Master' "$OLDVOL"

	$shutdowninit &	
fi
