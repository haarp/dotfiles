#!/bin/sh

# why wont -novtswitch work?!
xcommand="xinit -e "$@" -- :1 -ac -reset -terminate -quiet -nolisten tcp -to 15 -novtswitch"
#xcommand="xinit -e "$@" -- :1 -ac vt08"

$xcommand

if [ $(pgrep -f "$xcommand") ]; then
	pkill -f "$xcommand"
fi
