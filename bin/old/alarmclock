#!/bin/sh

#{ sleep 90
#for i in `seq 0 5`; do
#	beep; sleep 0.3; beep; sleep 0.3; beep; sleep 4
#done } &

#OLDVOL=$(amixer -c 0 get 'Master' | tail -n1 | awk '{print $3}')
#amixer -c 0 set 'Master' 100%

/usr/bin/cvlc /home/haarp/misc/alarmshaker.mp3 vlc://quit

#amixer -c 0 set 'Master' "$OLDVOL"
